# 單字大挑戰 — 專案交接說明

> **給新對話的 Claude：** 這是一個持續進行中的專案。請先讀完這份說明，再讀我一併上傳的 `vocab_quiz_v7_NEW.html`（那份 HTML 就是完整的程式碼與題庫，是唯一的真實來源）。

---

## 一、這是什麼

一個給學生用的英文單字測驗網頁（單一 HTML 檔，可直接發布成連結分享）。單字來自中文英語教材《Chapter 1 基礎 1,200 單字》，依 Topic 編號分單元。

---

## 二、目前狀態

| 項目 | 內容 |
|---|---|
| 最新檔案 | `vocab_quiz_v7_NEW.html` |
| 版本標記 | v7（頁面最上方有紅色橫幅） |
| 目前考試範圍 | Topic 30–34，共 **140 字** |
| 總複習題庫 | 累積 **775 字**（Topic 04–34 教過的全部） |
| 成績回報 | 程式碼已寫好但**關閉中**（老師說「先不回報」） |

目前範圍的五個單元：
- `auxiliaries` BE & AUX Be動詞&助動詞（10 字）
- `prepositions` PREPOSITIONS 介系詞（37 字）
- `conjunctions` CONJUNCTIONS 連接詞（8 字）
- `interjections` INTERJECTIONS 感歎詞（5 字）
- `nouns` NOUNS 其他名詞（80 字）

---

## 三、功能規格（請勿更動）

### 三種模式
1. **平常測驗練習**（吊人遊戲）— 可自選單元／題型／題數；**答錯滿 5 題立刻結束**（強制，不可調整）
2. **正式考試** — **完全不可調整設定**：全部單元、全部題型、固定 30 題；結束後給成績單（正確率、等第 A–F、詳細檢討，答錯的排前面）
3. **總複習** — 從「過去教過的所有單元」（REVIEW_WORDS，目前 775 字）出題；可選題型與題數

### 三種題型（全部都是四選一選擇題）
- **英翻中**：看英文單字選中文意思
- **中翻英**：看中文意思選英文單字
- **克漏字**：給一個例句、單字被挖空，選出正確單字填入（**不是**挖字母）

### 其他規則
- 作答中可以「返回主頁」（連按兩次確認，因為沙盒環境 `confirm()` 會被封鎖，**不要改用 confirm()**）
- 錯題會存進「我的錯題紀錄」，可用「錯題重練」消化（答對一次扣 1 次）
- 出題有加權：之前答錯過的字更容易再被抽到
- 正式考試 30 題會**平均分配到各單元**，避免大單元洗版

---

## 四、更新範圍時的標準流程（很重要）

老師會拍教材照片，說「範圍換成這些」。請這樣做：

1. **整批取代** `WORDS` 物件（不是累加），並更新 `CAT_LABEL`、`CAT_COLOR`、`.cat-tab[data-cat=...]` 的 CSS、以及副標題的 Topic 範圍
2. **同步把新單字併入 `REVIEW_WORDS`**（總複習題庫），併入前要查重，跳過已存在的英文單字或中文意思
3. **務必驗證**（每次都要跑）：
   - 沒有重複的英文單字
   - 沒有重複的中文意思 ← 這點最關鍵，否則會出現「兩個選項都對」
   - 每個單字的例句都含有 `___` 空格
   - JS 語法可正常 parse
4. 易混淆的字要用中文意思區分清楚。已處理過的例子：
   `job 工作（職位）` / `work 工作（做事）`、`shop 商店` / `store 店鋪`、
   `draw 畫；素描` / `paint 油漆；繪畫`、`big 大` / `large 大的`、
   `quiz 小考` / `test 測驗`、`problem 難題` / `question 問題（提問）`、
   `will 將；會` / `shall 將（提議）`、`garbage 垃圾` / `trash 廢棄物`

---

## 五、輸出方式（老師特別要求）

⚠️ **每次更新都要輸出「新檔名」的檔案**，例如 `vocab_quiz_v8_NEW.html`、`v9`…依序遞增。

原因：之前重複用同一個檔名，導致老師 Publish 後連結一直顯示舊版本。換新檔名 + 頁面上放明顯的版本橫幅（紅底白字，寫版本號與範圍），老師才能一眼確認是新版。

---

## 六、已知限制

- 每次換新檔名發布 → 會產生**新連結**，學生舊連結上的錯題紀錄**不會轉移**
- 錯題紀錄用的是 artifact 持久化儲存，**發布後才生效**，且免費方案可能無法使用
- **絕對不要 Unpublish**：一旦下架，所有學生存的錯題紀錄會被永久刪除，且同一個 artifact 無法重新發布

---

## 七、歷次範圍紀錄

| 批次 | 範圍 | 主要單元 |
|---|---|---|
| 1 | Topic 04–09 | 健康、稱謂、家庭、數字、時間、貨幣 |
| 2 | Topic 07–10 | 數字、時間、貨幣、食物&飲品 |
| 3 | Topic 11–15 | 餐具、服裝&飾品、顏色、運動&嗜好 |
| 4 | Topic 15–17 | 住宅、家具、電器、校園、文具、科目 |
| 5 | Topic 17–21 | 地點、場所建築、大眾運輸、尺寸測量、國家語言 |
| 6 | Topic 22–25 | 假日節慶、職業、天氣大自然、地理詞彙 |
| 7 | Topic 30–34 | Be動詞&助動詞、介系詞、連接詞、感歎詞、其他名詞 ← **目前** |

以上每批的單字都已經合併進 `REVIEW_WORDS`（總複習題庫），完整內容就在 HTML 檔案裡，不需要重新輸入。
