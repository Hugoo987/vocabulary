# 單字大挑戰 — 專案交接說明

> **給新對話的 Claude：** 這是一個持續進行中的專案。請先讀完這份說明，再讀 repo 裡的 `index.html`（那份 HTML 就是完整的程式碼與題庫，是唯一的真實來源）。

---

## 一、這是什麼

一個給學生用的英文單字測驗網頁（單一 HTML 檔，可直接發布成連結分享）。單字來自中文英語教材《Chapter 1 基礎 1,200 單字》，依 Topic 編號分單元。

---

## 二、目前狀態

| 項目 | 內容 |
|---|---|
| 最新檔案 | `index.html`（單一真實來源） |
| 版本標記 | v10（頁面最上方有紅色橫幅） |
| 發布方式 | GitHub Pages：https://hugoo987.github.io/vocabulary/ |
| 目前考試範圍 | 常見形容詞＋常見副詞，共 **91 字** |
| 總複習題庫 | 累積 **1014 字**（教過的全部） |
| 身分區分 | 學生／老師，各有獨立紀錄（v8 新增） |
| 跨裝置 | 自動同步（Firebase，見 `SETUP-FIREBASE.md`）＋回報碼備援 |
| 紀錄儲存 | `localStorage`，key `vocabQuiz:v2` |
| 成績回報 | 程式碼已寫好但**關閉中**（老師說「先不回報」） |

目前範圍的兩個單元：
- `adjectives2` ADJECTIVES 常見形容詞（58 字）
- `adverbs` ADVERBS 常見副詞（33 字）

其中 57 個形容詞是從上一批的 `adjectives` 單元留用的（中文與兩句例句都沿用）。

---

## 三、功能規格（請勿更動）

### 三種模式
1. **平常測驗練習**（吊人遊戲）— 可自選單元／題型／題數；**答錯滿 5 題立刻結束**（強制，不可調整）
2. **正式考試** — **完全不可調整設定**：全部單元、全部題型、固定 40 題（平均分配到各單元）；結束後給成績單（正確率、等第 A–F、詳細檢討，答錯的排前面）
3. **總複習** — 從「過去教過的所有單元」（REVIEW_WORDS，目前 775 字）出題；可選題型與題數

### 文章專區（v9 新增）
- 主頁第四個入口「文章閱讀」，三篇約 300 字的短文，每篇挖 12–14 個空格
- 讀完給成績單，答錯的字進錯題紀錄，題型記為 `article`（文章填空）
- 文章來源是 `data/articles.json`，`[[word]]` 是挖空記號，跑
  `node tools/apply-articles.js` 寫進 index.html
- **挖空的字一定要在題庫裡**（驗證器會擋），否則干擾選項生不出來；
  文章其他地方可以有超綱字，這是老師同意的

### 三種題型（全部都是四選一選擇題）
- **英翻中**：看英文單字選中文意思
- **中翻英**：看中文意思選英文單字
- **克漏字**：給一個例句、單字被挖空，選出正確單字填入（**不是**挖字母）

### 身分與紀錄系統（v8 新增，請勿更動行為）
- 進站先選身分：**我是學生／我是老師**，選過就記住，可用「切換身分」更換
- 學生和老師**各有一本獨立紀錄**——老師自己試做題目不可以混進學生的統計
- 老師模式多一個「學生學習報告」：常錯單字、各單元弱點、各題型錯誤分布、
  歷次成績、清除學生紀錄（按兩次確認）
- 紀錄用 `localStorage`（key `vocabQuiz:v2`），**不要改回 `window.storage`**——
  那個 API 只存在於已發布的 Claude artifact，在 GitHub Pages 上完全無效

### 其他規則
- 作答中可以「返回主頁」（連按兩次確認，因為沙盒環境 `confirm()` 會被封鎖，**不要改用 confirm()**）
- 錯題會存進「我的錯題紀錄」，可用「錯題重練」消化（答對一次扣 1 次），題數可選 10／20／30／全部
- 錯題重練用 `retrainPool()` 查找（考試範圍＋總複習題庫）。**不要改回 `fullPool()`**——
  那只含目前考試範圍，會讓總複習答錯的字（占題庫 71%）永遠練不掉，卡在紀錄裡清不掉
- `pickDistractors` 會避開「語意重疊」的干擾項（如 `USA 美國` vs `America 美國；美洲`），
  不只是避開完全相同的字串
- 出題有加權：之前答錯過的字更容易再被抽到
- 正式考試 40 題會**平均分配到各單元**（目前 2 單元各 20 題），避免大單元洗版。
  保底題數 = `min(總題數 ÷ 單元數, 最小單元字數)`，剩下的再依單元大小分配

---

## 四、更新範圍時的標準流程（很重要）

老師會拍教材照片，說「範圍換成這些」。請這樣做：

1. **整批取代** `WORDS` 物件（不是累加），並更新 `CAT_LABEL`、`CAT_COLOR`、`.cat-tab[data-cat=...]` 的 CSS、以及副標題的 Topic 範圍
2. **同步把新單字併入 `REVIEW_WORDS`**（總複習題庫），併入前要查重，跳過已存在的英文單字或中文意思
3. **務必驗證**（每次都要跑）：
   - 沒有重複的英文單字
   - 沒有重複的中文意思 ← 這點最關鍵，否則會出現「兩個選項都對」
   - 語意重疊警告（如 `big 大` vs `large 大的`）：出題時會自動避開，不會真的兩個都對，
     但看到警告代表值得把中文分清楚
   - 每個單字的例句都含有 `___` 空格
   - 每個單字至少 2 句例句，且兩句**結構要夠不同**（驗證器用詞彙重疊率 ≥0.35 擋下
     「只換幾個字」的假第二句）。新增單字時記得補 `data/alt-sentences.json`
   - JS 語法可正常 parse
4. 易混淆的字要用中文意思區分清楚。已處理過的例子：
   `job 工作（職位）` / `work 工作（做事）`、`shop 商店` / `store 店鋪`、
   `draw 畫；素描` / `paint 油漆；繪畫`、`big 大` / `large 大的`、
   `quiz 小考` / `test 測驗`、`problem 難題` / `question 問題（提問）`、
   `will 將；會` / `shall 將（提議）`、`garbage 垃圾` / `trash 廢棄物`、
   `hear 聽見` / `listen 聽`、`make 製作` / `do 做`、
   `become 成為` / `be 成為（be動詞）` / `get 變成`、
   `sell 賣` / `sale 特價；出售`、`able 有能力的` / `can 能`
   `miss 錯過；小姐` ← 同一個英文字，兩個意思都教過（Topic 35 動詞／Topic 05 稱謂），
   合併成一個解釋，避免同一個字在考試與總複習的答案不一致
   `once 一次` / `ever 曾經`、`away 遠離` / `leave 離開`、`no 沒有（任何）` / `without 沒有`

   ⚠️ **同義詞群組**：字串不同但學生眼中同義的中文（例如 `也許`／`或許`），
   單靠字串比對抓不到。`index.html` 的 `SYNONYM_GROUPS` 與 `tools/validate.js` 裡
   有一份**內容相同**的清單，出題時會避開、驗證時會警告。新增同義字要**兩邊都改**

5. ⚠️ **例句裡的押韻字是刻意的，不要當成錯字改掉**。名詞單元 80 句中有 52 句（65%）
   在例句放了押韻或形近字當記憶輔助：`corn`→`corner`、`cast`→`castle`、`cream`→`dream`、
   `enter`→`center`、`fell`→`bell`、`whale`→`sale`。看起來突兀是設計，不是筆誤。

---

## 五、輸出方式

⚠️ 改成 GitHub Pages 發布之後，**不再需要每次換新檔名**。

原本要換檔名，是因為 artifact 重複用同一個檔名會讓連結顯示舊版本。GitHub Pages 沒有這個問題：
網址固定是 https://hugoo987.github.io/vocabulary/ ，push 到預設分支就會自動重新部署到同一個網址，
學生的連結永遠不用換。

**還是要做的**：每次更新都把頁面最上方的紅色版本橫幅（紅底白字，寫版本號與範圍）往上加一號，
老師才能一眼確認自己看到的是新版。目前是 v8。

---

## 六、已知限制

- 紀錄跟著**裝置＋瀏覽器**走。學生換手機、換瀏覽器，或清掉網站資料，紀錄就沒了
- 跨裝置有兩條路：`SYNC_CONFIG` 設定好之後會自動同步（Firebase Realtime Database 的
  REST 介面，不載入任何 SDK）；沒設定時用回報碼手動傳。兩者共用 `buildPayload()`／
  `payloadToProfile()` 的資料格式，只帶英文字、次數、題型與日期，中文與單元由
  接收端的題庫查回來（30 個錯字約 1200 字元）
- **自動同步在 Claude artifact 版本上不會運作**——那個環境的 CSP 擋掉所有外部連線。
  artifact 版本請用回報碼
- 資料庫網址與 classKey 會出現在公開 repo 的原始碼裡。內容只有單字與分數、無個資，
  風險低；classKey 設長一點的隨機字串即可
- 無痕視窗不會保留紀錄（頁面會顯示提示，功能仍可正常使用）
- 另外有一個 Claude artifact 版本，內容相同但**連結與紀錄是分開的**。
  **絕對不要 Unpublish 那個 artifact**：一旦下架，存在裡面的紀錄會永久刪除，且無法重新發布

---

## 七、歷次範圍紀錄

| 批次 | 範圍 | 主要單元 |
|---|---|---|
| 1 | Topic 04–09 | 健康、稱謂、家庭、數字、時間、貨幣 |
| 2 | Topic 07–10 | 數字、時間、貨幣、食物&飲品 |
| 3 | Topic 11–15 | 餐具、服裝&飾品、顏色、運動&嗜好 |
| 4 | Topic 15–17 | 住宅、家具、電器、校園、文具、科目 |
| 5 | Topic 17–21 | 地點、場所建築、大眾運輸、尺寸測量、國家語言 |
| 6 | Topic 22–25 | 假日節慶、職業、天氣大自然、地理詞彙 |
| 7 | Topic 30–34 | Be動詞&助動詞、介系詞、連接詞、感歎詞、其他名詞 |
| 8 | Topic 34–36 | 其他名詞（留用）、動詞①②③、其他形容詞 |
| 9 | 詞性總整理 | 常見形容詞、常見副詞 ← **目前** |

以上每批的單字都已經合併進 `REVIEW_WORDS`（總複習題庫），完整內容就在 HTML 檔案裡，不需要重新輸入。
