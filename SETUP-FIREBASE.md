# 開啟自動同步（Firebase）

設定完成後，**學生做完測驗會自動上傳，老師打開報告頁就看到最新紀錄**，雙方都不用再手動傳回報碼。

免費，不需要信用卡。用你現有的 Google 帳號登入即可。大約 10 分鐘。

---

## 第一步：建立 Firebase 專案

1. 打開 https://console.firebase.google.com/
2. 用你的 Google 帳號登入
3. 按 **「建立專案」**，名稱隨便取（例如 `vocab-quiz`）
4. 問你要不要啟用 Google Analytics → **選「不要」**（用不到，也省得多設定）
5. 等它建立完成，按「繼續」

## 第二步：建立資料庫

1. 左邊選單找到 **「建構 Build」→「Realtime Database」**
   （⚠️ 不是 Firestore Database，要選 **Realtime** 那個）
2. 按 **「建立資料庫」**
3. 位置選 **`asia-southeast1`**（新加坡，離台灣最近）
4. 安全性規則選 **「以測試模式啟動」**，按啟用

## 第三步：設定安全性規則

測試模式 30 天後會自動失效，所以要換成固定規則。

1. 進到 Realtime Database 頁面，點上方的 **「規則 Rules」** 分頁
2. 把整個內容換成下面這段，按 **「發布」**：

```json
{
  "rules": {
    "vocab": {
      "$key": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['v','w'])"
      }
    }
  }
}
```

## ✅ 已完成設定

本站已填入以下設定（`index.html` 的 `SYNC_CONFIG`）：

| 項目 | 值 |
|---|---|
| 資料庫網址 | `https://vocab-fb8ae-default-rtdb.asia-southeast1.firebasedatabase.app` |
| classKey | `lee-p9f4yyn51s` |

資料會存在資料庫的 `/vocab/lee-p9f4yyn51s` 路徑下。

要換路徑的話，改 `index.html` 裡的 `classKey` 即可（換了之後舊資料就讀不到了，
要請學生重新做一次測驗上傳）。

---
## 設定完成後會怎麼運作

| 誰 | 做什麼 | 結果 |
|---|---|---|
| 學生 | 做完任何一種測驗 | 自動上傳，結果畫面顯示「☁️ 已同步給老師」 |
| 學生 | 按「清除紀錄」 | 雲端也一起清掉 |
| 老師 | 打開「查看報告」 | 自動抓最新資料 |
| 老師 | 按「🔄 重新整理」 | 立刻再抓一次 |

網路不通時不會卡住作答，紀錄仍然存在本機，下次做完測驗會再試一次上傳。

---

## 要知道的三件事

**1. 這個網址是公開的。** repo 是公開的，所以任何人看網頁原始碼都能找到你的資料庫網址和路徑。理論上有人可以寫入亂資料。

實際風險很低 —— 裡面只有「哪些英文單字答錯幾次」和分數，沒有姓名或任何個人資料。真的被寫壞了，讓學生再做一次測驗就會蓋回正確的資料。

如果你介意，最簡單的作法是把 `classKey` 設成很長的隨機字串（別人不會去翻你的 repo，也就猜不到）。

**2. 免費額度綽綽有餘。** Firebase 免費方案每月 10GB 傳輸量。這個用途一次同步大約 2KB，一天同步 100 次也只用掉 6MB／月。

**3. 在 Claude artifact 版本上無法運作。** 那個環境會擋掉所有外部連線。artifact 版本仍然可以用回報碼。GitHub Pages 版本不受影響。

---

## 沒設定也沒關係

`enabled: false`（預設）時，同步功能完全不啟用，也不會顯示任何相關畫面。
「產生回報碼／貼上回報碼」永遠都在，可以當作備援 —— 網路有問題時仍然能把紀錄搬過去。
